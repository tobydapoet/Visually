export enum InteractionType {
  LIKE = 'LIKE',
  UNLIKE = 'UNLIKE',
  SHARE = 'SHARE',
  UNSHARE = 'UNSHARE',
  COMMENT = 'COMMENT',
}

export enum StoryInteractionType {
  LIKE = 'LIKE',
  UNLIKE = 'UNLIKE',
}
