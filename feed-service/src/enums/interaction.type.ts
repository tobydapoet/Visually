export enum InteractionType {
  LIKE = 'LIKE',
  UNLIKE = 'UNLIKE',
  SHARE = 'SHARE',
  UNSHARE = 'UNSHARE',
  COMMENT = 'COMMENT',
}

export enum InteractionContentType {
  LIKE = 'LIKE',
  COMMENT = 'COMMENT',
  SHARE = 'SHARE',
}
