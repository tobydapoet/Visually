package com.example.service.repositories;

import com.example.service.entities.AdTarget;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AdTargetRepository extends JpaRepository<AdTarget,Long> {
}
